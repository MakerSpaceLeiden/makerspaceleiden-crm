Subject: [makerbot] Terminal {{ terminal.name }} accepted
L.S.

Just to let you know; the terminal:

       name: {{ terminal.name }}
fingerprint: {{ terminal.fingerprint }}
       date: {{ terminal.date }}

has been accepted/added as via a swipe by tag #{{ tag.id }} which
is owned by {{ tag.owner }}.

Your friendly makerbot.
-- 
Makerspace Leiden, internal payment notifcations.
